name: build

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install npm
      run : npm install
      
  #Вообще-то это не работает, надо прописать нормальную команду сборки и деплоя на маркетплейс       
    - name: Run build
      run : npm build
