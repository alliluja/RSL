{
	"scopeName": "MLcomment.injection",
	"injectionSelector": "L:comment.block.rsl",
	"patterns": [
		{
			"include": "#strHighlight"
		}

	],
	"repository": {
		"strHighlight": {
			"match": "(?i)`[\\w\\s]{1,}`",
			"name": "string.quoted.single.rsl"
		}
	}
}
